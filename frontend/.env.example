# ── Backend API URL ───────────────────────────────
# Development
BACKEND_URL=http://localhost:4000

# Production — set to your deployed backend URL
# BACKEND_URL=https://your-backend.railway.app

# ── Docker only ───────────────────────────────────
# Set to "standalone" when building Docker image
# NEXT_OUTPUT=standalone
